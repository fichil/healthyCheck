<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cykj.mapper.MenuMapper">


    <select id="getMenusByRoleId" resultType="com.cykj.pojo.Menu">
        SELECT Menu.*
        FROM Menu
        WHERE menu_id IN (SELECT menu_id FROM Permission WHERE role_id = #{roleId}) and is_deleted = 0;
    </select>
</mapper>