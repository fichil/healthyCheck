<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cykj.mapper.OrderDetailMapper">

    <!--    添加套餐详情-->
    <insert id="createOrderDetail" parameterType="com.cykj.pojo.OrderDetail">
        INSERT INTO Order_Detail (order_number, package_id,type)
        VALUES (#{orderNumber},#{packageId},#{type})
    </insert>

</mapper>